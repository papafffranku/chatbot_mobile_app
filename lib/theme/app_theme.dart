// lib/theme/app_theme.dart
import 'package:flutter/material.dart';

class AppTheme {
  static const bg = Color(0xFF0a0913);
  static const panel = Color(0xFF1d1936);
  static const bubbleUser = Color(0xFF282442);
  static const accent = Color(0xFF5DADE2);
  static const radius = 30.0;
}
